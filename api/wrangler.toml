name = "saas-api"
main = "src/index.ts"
compatibility_date = "2024-09-23"

# ---- KV (account_id は書かない) ----
[[kv_namespaces]]
binding = "LINE_BOOKING"
id = "22103efd0bd04365a2ec050a24b3dbc4"

# ---- Durable Objects (ロック用) ----
[[durable_objects.bindings]]
name = "SLOT_LOCK"
class_name = "SlotLock"

# ---- Migrations（Freeは new_sqlite_classes が必須）----
[[migrations]]
tag = "v2-slot-lock"               # 既存と被ったら v3-slot-lock に変えてOK
new_sqlite_classes = ["SlotLock"]
